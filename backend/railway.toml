[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# startCommand를 제거하여 Dockerfile의 CMD 사용
# Dockerfile의 CMD가 이미 PORT 환경 변수를 올바르게 처리함
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
